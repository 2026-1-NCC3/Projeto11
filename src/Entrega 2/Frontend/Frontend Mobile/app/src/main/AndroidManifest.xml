<?xml version="1.0" encoding="utf-8"?>
<!-- ============================================================
     AndroidManifest.xml
     Declara todas as Activities, permissões e configurações
     do app Maya RPG.
     ============================================================ -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissões necessárias -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".MayaApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo_maya"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_maya"
        android:supportsRtl="true"
        android:theme="@style/Theme.MayaRPG"
        android:usesCleartextTraffic="true">

        <!-- Tela de Splash (primeira a abrir) -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.MayaRPG.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tela de Login -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Tela de Cadastro -->
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Tela de Recuperação de Senha -->
        <activity
            android:name=".activities.ForgotPasswordActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Tela de Aceite LGPD -->
        <activity
            android:name=".activities.LgpdActivity"
            android:exported="false" />

        <!-- Activity principal com bottom navigation -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />

        <!-- Tela de detalhes de um exercício -->
        <activity
            android:name=".activities.ExerciseDetailActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity" />

        <!-- Worker para notificações agendadas -->
        <receiver
            android:name=".services.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Receiver para Lembrete Diário (Alarme) -->
        <receiver
            android:name=".activities.ReminderReceiver"
            android:exported="false" />

    </application>
</manifest>